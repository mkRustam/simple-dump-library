version: "3.8"

services:
  postgres-security-example:
    image: postgres:17.5
    environment:
      - POSTGRES_DB=spring_app_security_db
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
    ports:
      - "5432:5432"